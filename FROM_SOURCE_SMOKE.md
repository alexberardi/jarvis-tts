# From-source cutover smoke test (THROWAWAY — do not merge)

Opened only to confirm the integration-trigger -> from-source lane chain fires
end-to-end on a real PR (dispatch token + comment-back). Close without merging.
