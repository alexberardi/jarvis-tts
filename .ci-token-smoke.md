# CI token smoke test (throwaway)

Throwaway marker to open a no-op PR that fires `integration-trigger.yml`, so we can
confirm the `INTEGRATION_DISPATCH_TOKEN` (here) and `INTEGRATION_COMMENT_TOKEN`
(on jarvis-integration-tests) still authenticate after the 2026-06-22 PAT recycle.

DO NOT MERGE — this PR is opened and closed for verification only.
