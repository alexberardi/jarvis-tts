version: '3.9'

services:
  jarvis-tts:
    build: .
    ports:
      - "${TTS_PORT:-8009}:${TTS_PORT:-8009}"
    volumes:
      - ./app:/app/app
      - ./.env:/app/.env
      - ../jarvis-log-client:/app/jarvis-log-client
      - ../jarvis-config-client:/app/jarvis-config-client
      - ../jarvis-auth-client:/app/jarvis-auth-client
      - ../jarvis-settings-client:/app/jarvis-settings-client
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
    extra_hosts:
      - "${DOCKER_HOST_ALIAS:-host.docker.internal}:host-gateway"
    command: >
      sh -c "pip install -q -e /app/jarvis-log-client -e /app/jarvis-config-client -e /app/jarvis-auth-client -e /app/jarvis-settings-client &&
             uvicorn app.main:app --host 0.0.0.0 --port ${TTS_PORT:-8009} --reload"

  postgres:
    profiles: ["standalone"]
    image: postgres:15
    environment:
      POSTGRES_DB: jarvis_tts
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - tts-pg-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 3s
      retries: 5

volumes:
  tts-pg-data:
