version: '3.9'

services:
  jarvis-tts:
    build: .
    ports:
      - "${TTS_PORT:-7707}:${TTS_PORT:-7707}"
    volumes:
      - ./app:/app/app
      - ./.env:/app/.env
      - ../jarvis-log-client:/app/jarvis-log-client
      - ../jarvis-config-client:/app/jarvis-config-client
      - ../jarvis-auth-client:/app/jarvis-auth-client
      - ../jarvis-settings-client:/app/jarvis-settings-client
      - jarvis-tts-hf-cache:/app/models/hf_cache
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
      - HF_HOME=/app/models/hf_cache
    extra_hosts:
      - "${DOCKER_HOST_ALIAS:-host.docker.internal}:host-gateway"
    ulimits:
      nofile:
        soft: 65536
        hard: 65536
    command: >
      sh -c "pip install -q -e /app/jarvis-log-client &&
             pip install -q -e /app/jarvis-config-client &&
             pip install -q -e /app/jarvis-auth-client &&
             pip install -q -e /app/jarvis-settings-client &&
             uvicorn app.main:app --host 0.0.0.0 --port ${TTS_PORT:-7707} --reload"

  postgres:
    profiles: ["standalone"]
    image: postgres:15
    environment:
      POSTGRES_DB: jarvis_tts
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - tts-pg-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 3s
      retries: 5

volumes:
  tts-pg-data:
  jarvis-tts-hf-cache:
